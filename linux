RAW_VERSION="-1234-87654321-main"

# Extract prefix and suffix only: YYYYxxxQn-main
CLEAN_VERSION=$(echo "$RAW_VERSION" | sed -E 's/^([0-9]{7}Q[0-9])-.*-([a-zA-Z/_-]+)$/\1-\2/')

echo "Clean version: $CLEAN_VERSION"

# Use this when running Maven
mvn clean install -Drevision="$CLEAN_VERSION"
